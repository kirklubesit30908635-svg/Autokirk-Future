# Kernel Principles

- Kernel is the only mutation authority.
- All meaningful state changes must follow:
  event -> obligation -> resolution -> receipt
- UI is projection only.
- AI is advisory only.
- Receipts are proof.
- External input is never trusted as canonical truth without kernel validation.
